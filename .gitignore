# Секреты: токен бота, id чатов, белый список людей.
# Репозиторий публичный — эти значения не должны попадать в историю.
src/02-settings/local.js

# Сборка с подставленными секретами, её импортируют в хаб
dist/
