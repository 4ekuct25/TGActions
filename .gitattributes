# Экспорты сценариев Sprut.hub и извлечённый из них код хранятся байт-в-байт.
# В GLOBAL-сценариях переводы строк — CRLF; любая нормализация EOL ломает
# обратную сборку (tools/scenarios.py check).
*.json -text
src/*.js -text
